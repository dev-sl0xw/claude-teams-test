# AWS SDK for Python
# -- 왜: boto3는 AWS 서비스와 프로그래밍 방식으로 상호작용하기 위한 공식 Python SDK입니다.
# CDK가 인프라를 "정의"한다면, boto3는 인프라를 "운영"하는 도구입니다.
# 질문: CDK와 boto3의 역할 차이는 무엇인가? 둘 다 AWS를 다루는데 왜 두 개가 필요한가?
boto3>=1.35.0

# AWS CLI 설정 도우미
# -- 왜: botocore는 boto3의 핵심 라이브러리로, 자격 증명 관리와 리전 설정을 담당합니다.
botocore>=1.35.0

# JSON 스키마 검증
# -- 왜: CloudFormation 템플릿의 유효성을 프로그래밍 방식으로 검증할 때 사용합니다.
jsonschema>=4.23.0

# 터미널 출력 포매팅
# -- 왜: 학습 스크립트의 실행 결과를 보기 좋게 표시하기 위한 도구입니다.
rich>=13.9.0
